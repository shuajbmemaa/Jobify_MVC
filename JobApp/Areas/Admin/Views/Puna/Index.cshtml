@model List<Puna> 
<div class="container">
        <div class="row pt-4 pb-3">
            <div class="col-6">
                <h2 class="text-primary">Puna List</h2>
            </div>
            <div class="col-6 text-end">
                <a asp-controller="Puna" asp-action="Create" class="btn btn-primary">
                Shto nje pune <i class="bi bi-patch-plus"></i>
                </a>
            </div>
        </div>

<table class="table table-bordered table-striped">
    <thead>

    <tr>
        <th>Emri i Pozites</th>
        <th>Pershkrimi</th>
        <th>Kerkesat</th>
        <th>Lokacioni</th>
        <th>Image</th>
        <th>Kategoria</th>

    </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td>@obj.Emri_Pozites</td>
                <td>@obj.Pershkrimi</td>
                <td>@obj.Kerkesat</td>
                <td>@obj.Lokacioni</td>
                <td>@obj.ImageUrl</td>
                <td></td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Puna" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil"></i> Edit
                        </a>
                            <a asp-controller="Puna" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash3"></i> Delete
                            </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>